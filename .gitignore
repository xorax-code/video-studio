# ── Secrets — NEVER commit ──────────────────────────────────────────
# Generated at Netlify build time from env vars; may contain keys.
supabase-config.js

# ── Dependencies & build artifacts ──────────────────────────────────
node_modules/
app.obfuscated.html
_build.txt

# ── Editor / OS junk ────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log

# ── Backups & scratch copies ────────────────────────────────────────
*.bak
*.bak_*
*.tail.html

# ── Large test / reference media (keep the 6 landing clips) ─────────
*.mp4
!clip-1.mp4
!clip-2.mp4
!clip-3.mp4
!clip-4.mp4
!clip-5.mp4
!clip-6.mp4
cmp/
f0*.jpg
